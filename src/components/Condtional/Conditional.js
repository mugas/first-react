import React from "react";

function Conditional(props) {
  return <h1>Ta dam </h1>;
}
/*
  console.log(props.isLoading);

  if (props.isLoading === true) {
    return <h1>Loading...</h1>;
  } else {
    return <h1>Temp</h1>;
  }

  
  return(
      <div>
      {props.isLoading ? <h1>Loading...</h1> : <h1>Temp</h1> 
      </div>  
        )
}
*/
export default Conditional;
